/**
 * Displays a message on the STDOUT using console.log.
 * @param {String} msg The message to display.
 * @author Samuel <https://github.com/samcoded>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
